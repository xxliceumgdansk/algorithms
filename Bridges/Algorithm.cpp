#include "Algorithm.h"

Bridges::Bridges(vector<node*> _nodes, int _numberOfNodes)
{
    nodes = _nodes;
    numberOfNodes = _numberOfNodes;
}

Bridges::~Bridges()
{
    //dtor
}

vector<vector<int>> Bridges::Find() {
	vector<vector<int>> resultList;

    return resultList;
}
