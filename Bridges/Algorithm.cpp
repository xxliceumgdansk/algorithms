#include "Algorithm.h"

Bridges::Bridges(vector<node*> _graph, int _numberOfNodes)
{
    graph = _graph;
    numberOfNodes = _numberOfNodes;
}

Bridges::~Bridges()
{
    //dtor
}

vector<vector<int> > Bridges::Find() {
	vector<vector<int>> resultList;

    // Your algorithm code goes here.
    // Graph nodes are located in "graph" class field.
    // Happy coding!

    return resultList;
}
